# AGENTS

## Cursor Cloud specific instructions

This repository (`litre`) is currently empty — it contains only a `README.md` placeholder. There are no source files, dependencies, build systems, linters, test frameworks, or runnable services.

- **Branch convention:** The default branch is `Main` (capital M).
- **No build/test/lint commands exist yet.** When code is added, update this section with the relevant commands.
- **No services to start.** When services are introduced, document startup instructions here.
